#服务启动端口
server:  
  port: 8092

#数据库配置
jdbc:
  datasource:
    name: test
    url: jdbc:mysql://39.104.76.67:3086/oep
    username: test
    password: 09)(poPO
    # 使用druid数据源
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    filters: stat
    maxActive: 20
    initialSize: 1
    maxWait: 60000
    minIdle: 1
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: select 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxOpenPreparedStatements: 20

mybatis:
  mapper-locations: classpath:mappers/*.xml
  
#spring:
#  jackson:
#    time-zone: GMT+8
#    date-format: yyyy-MM-dd HH:mm:ss

# 日志输出
logging:
  level: 
    com.ibatis: INFO
    root: INFO
    org.apache: INFO
